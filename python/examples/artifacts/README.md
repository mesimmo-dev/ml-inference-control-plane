Curated, tiny examples of the `micp-eval.v1` artifact shape.

These are **fixtures for docs and tests**, not a live run. Regenerate
real output with `micp-eval evaluate` against a running `micp-api`.
Do not commit `python/artifacts/` dumps.
