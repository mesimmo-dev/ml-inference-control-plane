Bundled copies of `python/examples/artifacts/` for the workbench.

These are **curated modeled examples**, not live experiment dumps and not
empirical measurements. The workbench labels them as Python evaluation
artifacts with origin `modeled`. Regenerate live output with `micp-eval`.
